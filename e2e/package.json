{
  "name": "calvin-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "End-to-End-Tests (Playwright) für das Calvin-Frontend",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
